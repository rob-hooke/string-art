---
phase: 03-minimalist-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/index.css, src/main.jsx, src/StringArtGenerator.jsx]
autonomous: true
requirements: ["1.5", "2.3"]
must_haves:
  truths:
    - "UI uses a Bento Grid layout with modular cards"
    - "No inline styles remain for static elements in StringArtGenerator.jsx"
    - "All interactive elements have ARIA labels"
    - "Progress and status updates are announced via role='status'"
    - "Playback controls (Play/Pause, Seek) are functional and styled"
  artifacts:
    - path: "src/index.css"
      provides: "CSS variables and Bento Grid layout system"
    - path: "src/StringArtGenerator.jsx"
      provides: "Modularized UI with clean structure"
  key_links:
    - from: "src/main.jsx"
      to: "src/index.css"
      via: "import"
---

<objective>
Refine the String Art Generator UI to follow a minimalist "Bento Grid" aesthetic, extract styles to a dedicated CSS file, improve accessibility, and refine playback controls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phase3/RESEARCH.md
@src/StringArtGenerator.jsx
@src/main.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS Infrastructure & Style Extraction</name>
  <files>src/index.css, src/main.jsx, src/StringArtGenerator.jsx</files>
  <action>
    - Create `src/index.css` and define CSS variables in `:root` for:
      - Colors: `--bg-main`, `--panel-bg`, `--accent-red`, `--text-main`, `--text-dim`.
      - Spacing: `--gap-sm`, `--gap-md`, `--gap-lg`.
      - Borders: `--radius-md`, `--border-dim`.
    - Extract all styles from the internal `<style>` block in `StringArtGenerator.jsx` into `src/index.css` as semantic classes (e.g., `.btn`, `.panel`, `.progress-bar`).
    - Remove the `<style>` block from `StringArtGenerator.jsx`.
    - Import `src/index.css` in `src/main.jsx`.
  </action>
  <verify>
    <automated>grep -q ":root" src/index.css && grep -q "import './index.css'" src/main.jsx && ! grep -q "&lt;style&gt;" src/StringArtGenerator.jsx</automated>
  </verify>
  <done>Styles moved to external CSS file with variable-driven infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Bento Grid Layout Refactor</name>
  <files>src/StringArtGenerator.jsx, src/index.css</files>
  <action>
    - Restructure `StringArtGenerator.jsx` to use a `.bento-grid` container.
    - Organize UI into "modular cards" for:
      - Header (Title/Subtitle)
      - Dimensions & Configuration
      - Image Upload & Controls
      - Canvas Preview
      - Instruction Steps
    - Replace all static inline `style={{...}}` attributes with semantic class names.
    - Implement the Bento Grid layout in `src/index.css` using `display: grid` and `grid-template-columns`. Ensure responsiveness (stacking on mobile).
  </action>
  <verify>
    <automated>grep -q "className=\"bento-grid\"" src/StringArtGenerator.jsx && ! grep -q "style={{ margin: '0 auto' }}" src/StringArtGenerator.jsx</automated>
  </verify>
  <done>UI layout refactored into a clean, responsive Bento Grid.</done>
</task>

<task type="auto">
  <name>Task 3: Accessibility & Progress Refinement</name>
  <files>src/StringArtGenerator.jsx</files>
  <action>
    - Add `aria-label` to all interactive elements (sliders, inputs, buttons).
    - Add `role="status"` to the progress indicator and step counter.
    - Add `aria-busy="true"` to the preview section when `isProcessing` is true.
    - Implement a loading state in the preview card (e.g., a `.is-loading` class that adds an overlay or reduces opacity) triggered by `isProcessing`.
  </action>
  <verify>
    <automated>grep -q "aria-label=" src/StringArtGenerator.jsx && grep -q "role=\"status\"" src/StringArtGenerator.jsx</automated>
  </verify>
  <done>UI accessibility improved and loading states clearly communicated.</done>
</task>

<task type="auto">
  <name>Task 4: Playback Controls Refinement</name>
  <files>src/StringArtGenerator.jsx, src/index.css</files>
  <action>
    - Refine the Play/Pause toggle and Seek bar (slider) implementation.
    - Ensure controls are styled consistently with the minimalist theme.
    - Ensure the Seek bar properly updates `currentStep` and the preview.
    - Use clear semantic buttons and input range for the seek bar.
  </action>
  <verify>
    <automated>grep -q "isPlaying" src/StringArtGenerator.jsx && grep -q "type=\"range\"" src/StringArtGenerator.jsx</automated>
  </verify>
  <done>Playback controls refined and integrated into the minimalist design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> CSS | CSS variables are static and defined in source code, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Local file paths | mitigate | Ensure no sensitive file paths are exposed in CSS/JS error logs. |
</threat_model>

<verification>
- No static inline styles in `StringArtGenerator.jsx`.
- Grid layout is functional and responsive.
- ARIA labels and live regions are active.
- Playback controls work as expected.
</verification>

<success_criteria>
- Phase 3 goal achieved: UI is minimalist, responsive, and accessible.
- Requirements 1.5 and 2.3 are fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-minimalist-ui/03-01-SUMMARY.md`
</output>
