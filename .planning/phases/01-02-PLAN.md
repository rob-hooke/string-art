---
phase: 01-stability
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/test/stability.test.jsx]
autonomous: true
requirements: [1.4]
must_haves:
  truths:
    - "Automated tests verify that parameter changes stop processing"
    - "Automated tests verify that rendering doesn't crash with stale data"
  artifacts:
    - path: "src/test/stability.test.jsx"
      provides: "Regression tests for Phase 1 stability fixes"
---

<objective>
Implement regression tests to ensure that the stability fixes in Phase 1 remain effective and to provide automated verification of the fix.

Purpose: Prevent regression of crash-inducing bugs and race conditions.
Output: New test file `src/test/stability.test.jsx` covering key stability scenarios.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phase1/RESEARCH.md
@src/StringArtGenerator.jsx
@src/test/setup.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stability regression test suite</name>
  <files>src/test/stability.test.jsx</files>
  <behavior>
    - Test 1 (Cancellation): Changing 'physicalWidth' during generation sets isProcessing to false.
    - Test 2 (Rendering Safety): Reducing 'nailCount' (via spacing) when a large 'stringPath' exists does not throw an error.
    - Test 3 (Clean Exit): generateStringArt always resets isProcessing even if aborted.
  </behavior>
  <action>
    - Create `src/test/stability.test.jsx`.
    - Use `@testing-library/react` to render `StringArtGenerator`.
    - Mock the canvas context if necessary (vitest usually handles basic canvas calls or they are no-ops in jsdom).
    - Implement the test cases described in the behavior section.
    - Specifically, use `fireEvent` or `userEvent` to trigger dimension changes during a simulated long-running generation.
  </action>
  <verify>
    <automated>npm test src/test/stability.test.jsx</automated>
  </verify>
  <done>Regression tests are passing and cover the critical bugs fixed in Plan 01.</done>
</task>

</tasks>

<success_criteria>
- All new stability tests pass.
- Test coverage for the generation loop and rendering effect is improved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability/01-02-SUMMARY.md`
</output>
