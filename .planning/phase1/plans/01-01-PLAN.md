---
phase: 01-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/StringArtGenerator.jsx, src/test/stability.test.jsx]
autonomous: true
requirements: [1.1, 1.2, 1.3, 1.4]
must_haves:
  truths:
    - "Automated tests are in place BEFORE implementation fixes (TDD)"
    - "Changing canvas dimensions immediately stops any running generation"
    - "Changing nail spacing resets the string path to prevent rendering crashes"
    - "isProcessing is reliably reset even if the generation loop is interrupted"
  artifacts:
    - path: "src/StringArtGenerator.jsx"
      provides: "Synchronized state and safe rendering"
    - path: "src/test/stability.test.jsx"
      provides: "Automated stability regression suite"
  key_links:
    - from: "src/StringArtGenerator.jsx"
      to: "src/test/stability.test.jsx"
      rel: "validated_by"
---

<objective>
Fix core stability issues in the StringArtGenerator component and establish a regression test suite.

Purpose: Prevent UI crashes and inconsistent states when users modify canvas dimensions or nail spacing.
Output: Robust component logic and a passing stability test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phase1/RESEARCH.md
@src/StringArtGenerator.jsx
@src/test/setup.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create stability regression test suite (initial failing state)</name>
  <files>src/test/stability.test.jsx</files>
  <behavior>
    - Test 1 (Cancellation): Changing 'physicalWidth' during generation sets isProcessing to false.
    - Test 2 (Rendering Safety): Reducing 'nailCount' (via spacing) when a large 'stringPath' exists does not throw an error.
    - Test 3 (Clean Exit): generateStringArt always resets isProcessing even if aborted.
  </behavior>
  <action>
    - Create `src/test/stability.test.jsx`.
    - Implement the test cases. They are expected to fail initially (TDD).
  </action>
  <verify>
    <automated>npm test -- src/test/stability.test.jsx</automated>
  </verify>
  <done>Regression test suite exists and correctly identifies the bugs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement robust loop control and cancellation</name>
  <files>src/StringArtGenerator.jsx</files>
  <behavior>
    - When generateStringArt is called, it should set isProcessing to true.
    - When processingRef.current becomes false, the loop should terminate immediately.
    - isProcessing and processingRef.current must be reset to false in a finally block.
  </behavior>
  <action>
    - Wrap the internal logic of generateStringArt in a try...finally block.
    - Move setIsProcessing(false) and processingRef.current = false into the finally block.
    - Add an explicit check for `if (!processingRef.current) return;` immediately after the `await new Promise(...)` call inside the loop to catch cancellations during the pause.
  </action>
  <verify>
    <automated>npm test -- src/test/stability.test.jsx</automated>
  </verify>
  <done>Generation loop is protected by try...finally and supports mid-loop cancellation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Parameter-Driven Cancellation Effect</name>
  <files>src/StringArtGenerator.jsx</files>
  <action>
    - Add a new useEffect that monitors [physicalWidth, physicalHeight, unit, nailSpacing, imageData].
    - Inside the effect:
      1. Set processingRef.current = false to signal cancellation to any running loop.
      2. Reset setIsProcessing(false).
      3. Clear setStringPath([]).
      4. Reset setCurrentStep(0).
  </action>
  <verify>
    <automated>npm test -- src/test/stability.test.jsx</automated>
  </verify>
  <done>Any parameter change safely aborts current work and resets volatile state.</done>
</task>

<task type="auto">
  <name>Task 3: Add boundary checks to rendering loops</name>
  <files>src/StringArtGenerator.jsx</files>
  <action>
    - In the rendering useEffect (around line 228 and 243):
      - Before calling ctx.moveTo(nails[line.from].x, ...) and ctx.lineTo(nails[line.to].x, ...), add a check:
        `if (nails[line.from] && nails[line.to]) { ... }`
  </action>
  <verify>
    <automated>npm test -- src/test/stability.test.jsx</automated>
  </verify>
  <done>Rendering loops are resilient to stale stringPath indices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input -> State | Parameters like width/height are sanitized on blur but used directly in calculations. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | DoS (ReDoS/Loop) | generateStringArt | mitigate | implemented cancellation and try...finally to prevent hung states. |
</threat_model>

<success_criteria>
- No console errors when changing dimensions during or after generation.
- UI remains responsive (buttons clickable) after multiple parameter changes.
- All stability tests in `src/test/stability.test.jsx` pass.
</success_criteria>

<output>
After completion, create `.planning/phase1/01-01-SUMMARY.md`
</output>
