---
phase: 02-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/services/pdfExportService.js, src/test/pdfExport.test.js]
autonomous: true
requirements: [REQ-1.4]

must_haves:
  truths:
    - "PdfExportService can generate a PDF blob/file"
    - "Instructions are rendered in a multi-column layout to save space"
    - "Project summary includes dimensions, nail count, and connections"
  artifacts:
    - path: "src/services/pdfExportService.js"
      provides: "PDF generation logic using jsPDF"
    - path: "src/test/pdfExport.test.js"
      provides: "Automated verification of PDF data preparation"
---

<objective>
Implement the core PDF generation service using jsPDF, focusing on the project summary page and the efficient multi-column layout for routing instructions.

Purpose: Provide a professional and printable guide for users to build their string art.
Output: `PdfExportService.js` and supporting tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase2/RESEARCH.md
@src/StringArtGenerator.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jsPDF and scaffold PdfExportService</name>
  <files>package.json, src/services/pdfExportService.js</files>
  <action>
    1. Install `jspdf@^2.5.1`.
    2. Create `src/services/pdfExportService.js`.
    3. Implement `generatePdf` function that accepts:
       - `projectData`: { physicalWidth, physicalHeight, unit, nailCount, actualSpacing, stringCount }
       - `stringPath`: Array of { from, to }
    4. Configure jsPDF with `unit: 'mm'` and `format: 'a4'`.
    5. Implement the first page: Project Summary and Nail Placement Guide (as per REQ-1.4 and RESEARCH.md).
  </action>
  <verify>
    <automated>npm list jspdf && node -e "require('./src/services/pdfExportService.js')"</automated>
  </verify>
  <done>jsPDF installed and service scaffolds with summary page logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Multi-Column Routing Logic</name>
  <files>src/services/pdfExportService.js, src/test/pdfExport.test.js</files>
  <behavior>
    - Test 1: Should correctly calculate column transitions (e.g., if y > PAGE_BOTTOM, move to next column).
    - Test 2: Should add a new page when all columns on the current page are full.
    - Test 3: Should handle large paths (e.g., 3000 steps) without crashing or overlapping text.
  </behavior>
  <action>
    Implement the manual layout loop in `pdfExportService.js` using constants for COLUMNS (3), MARGIN, and COLUMN_GAP. Use `doc.text()` for rendering steps (e.g., "1. 0 -> 42"). Handle pagination logic as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>npm test src/test/pdfExport.test.js</automated>
  </verify>
  <done>Multi-column routing instructions implemented and verified with tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Data → jsPDF | User-provided labels (if any) or dimensions are processed in-browser. No server-side boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | pdfExportService.js | mitigate | Ensure all numerical inputs are validated/sanitized before being used in coordinate math to prevent infinite loops or layout crashes. |
| T-02-02 | Information Disclosure | generated PDF | accept | PDF contains routing info which is public-intent; no PII involved. |
</threat_model>

<success_criteria>
- `PdfExportService` can generate a multi-page PDF with summary and routing steps.
- Routing steps use 3-column layout.
- Automated tests pass for layout logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-export/02-01-SUMMARY.md`
</output>
