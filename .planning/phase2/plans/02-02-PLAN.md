---
phase: 02-pdf-export
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/StringArtGenerator.jsx]
autonomous: false
requirements: [REQ-1.4]

must_haves:
  truths:
    - "User can download PDF from the UI after generating string art"
    - "UI shows a 'Generating...' state while PDF is being created"
    - "PDF content matches the current project configuration"
  artifacts:
    - path: "src/StringArtGenerator.jsx"
      provides: "UI button and integration with PdfExportService"
---

<objective>
Integrate the PdfExportService into the main UI and verify the end-to-end workflow, ensuring the user can easily export their instructions in PDF format.

Purpose: Complete the feature by making it accessible to the user.
Output: Updated UI with PDF export capability.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase2/RESEARCH.md
@.planning/phase2/02-01-SUMMARY.md
@src/StringArtGenerator.jsx
@src/services/pdfExportService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI Wiring and Integration</name>
  <files>src/StringArtGenerator.jsx</files>
  <action>
    1. Import `pdfExportService` in `StringArtGenerator.jsx`.
    2. Add a `isExportingPdf` state to handle the loading indicator.
    3. Implement `handleDownloadPdf` function that:
       - Sets `isExportingPdf` to true.
       - Gathers current project data (dimensions, nail count, etc.).
       - Calls `pdfExportService.generatePdf(projectData, stringPath)`.
       - Sets `isExportingPdf` to false.
    4. Add a "📄 Download PDF Instructions" button to the UI (near the existing export button).
    5. Show a "Generating PDF..." message or disabled state while exporting.
  </action>
  <verify>
    <automated>grep -q "pdfExportService" src/StringArtGenerator.jsx && grep -q "Download PDF Instructions" src/StringArtGenerator.jsx</automated>
  </verify>
  <done>PDF export button added and wired to the service.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-End Verification</name>
  <what-built>The full PDF export feature integration.</what-built>
  <how-to-verify>
    1. Open the application.
    2. Upload an image and generate string art.
    3. Click the "Download PDF Instructions" button.
    4. Open the downloaded PDF and verify:
       - Project summary page has correct dimensions and counts.
       - Routing instructions use the multi-column layout.
       - Layout remains readable for large paths (e.g., 2000+ steps).
       - Page numbering and headers are present (if implemented).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Service | State is passed to the service. Data is entirely client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service (Client) | StringArtGenerator.jsx | mitigate | PDF generation happens on the main thread; ensure it's triggered by user action only and provides visual feedback (loading state) to prevent multiple concurrent triggers. |
</threat_model>

<success_criteria>
- User can successfully download a PDF version of the routing instructions.
- The "Download PDF" button is clearly visible and functional.
- The generated PDF matches the project requirements and research recommendations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-export/02-02-SUMMARY.md`
</output>
